"""2번 모듈 공용 라이브러리.

이 파일이 있어야 정규 패키지가 된다. 없으면 파이썬이 네임스페이스 패키지로
취급하는데, 네임스페이스 패키지는 우선순위가 가장 낮아서 sys.path 어디에든
`core.py` 나 `core` 패키지가 하나라도 있으면 그쪽이 이긴다. 그러면
`from core.result import ...` 가 전부 import 실패로 죽는다.
PyPI 에 `core` 라는 이름의 패키지가 실제로 있다. (2026-09-23 테스트 중 발견)
"""
